/* 
1. Given an array, write a function to move all 0s to the end of it while maintaining the
relative order of the non-zero elements.
*/

package main
import "fmt"

func move_zeroesLast(num []int)  {
    var index int=0
    for i:=0; i<len(num); i++ {
        if num[i]!=0 {
                num[index],num[i]=num[i],num[index]
                index++
        }
    }
}
    
    
